# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Environments
.env
venv/
.venv/

# Caches
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Data: the full 28MB dataset is not committed (see README for how to obtain
# it). The 5,000-row stratified sample IS committed so a fresh deploy has a
# working portfolio for the stress and concentration endpoints.
data/*.csv
!data/portfolio_sample.csv

# Generated reports
reports/*.json
reports/*.png

# Model artifacts: the CURRENT model IS committed, so the container and CI can
# build from a clean clone. Older versions are not.
artifacts/*
!artifacts/LATEST
!artifacts/v20260820T235355Z/

# Jupyter
.ipynb_checkpoints/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
